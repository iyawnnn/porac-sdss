// Non-spatial Drizzle table definitions go here (tickets, reports, admins, etc.)
// starting in Phase 1. Tables with geometry columns (barangays, dem_points) are
// created and queried via lib/db/raw.ts instead — see that file for why.
